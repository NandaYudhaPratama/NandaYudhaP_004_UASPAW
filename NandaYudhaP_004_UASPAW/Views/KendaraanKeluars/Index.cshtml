@model IEnumerable<NandaYudhaP_004_UASPAW.Models.KendaraanKeluar>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NoPolisi)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tarif)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdParkirNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NoPolisi)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Tarif)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdParkirNavigation.IdParkir)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdParkir">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdParkir">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdParkir">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
